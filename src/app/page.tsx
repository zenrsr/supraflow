import { Button } from "@/components/ui/button";
import React from "react";

type Props = {};

const Home = (props: Props) => {
  return (
    <Button className="flex items-center justify-center">Hello World</Button>
  );
};

export default Home;
